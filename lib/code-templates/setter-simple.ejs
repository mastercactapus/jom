<%

function propString(props, idx) {
	var propName = props[idx];
	var propInt = parseInt(propName, 10);
	if (!/^[0-9]+$/.test(propName)) {
		%>["<%=propName%>"]<%
	} else {
		%>[<%=propName%>]<%
	}
} 

%>


(function(_object, value) {
	var object = _object;
	<% for (var i = 0; i < props.length-1; i++) { %>
		if (typeof object<%propString(props, i)%> !== "object") object<%propString(props, i)%> = {};
		object = object<%propString(props, i)%>;
	<% } %>
	object<%propString(props, props.length-1)%> = value;
})
